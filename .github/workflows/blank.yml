name: myFirstWorkflow

on:
  workflow_dispatch:

jobs:
  first_job:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Hello Ramniwash"

      - name: Print bye
        run: echo "Bye Ramniwash"
